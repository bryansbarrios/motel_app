import 'package:flutter/material.dart';

class MotelDetailScreen extends StatelessWidget {
  const MotelDetailScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Vista detalle de motel"),
    );
  }
}